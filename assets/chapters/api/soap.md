# SOAP: Simple Object Access Protocol

Underlying protocol: almost anything, HTTP/HTTPS/SMTP… 
Uses XML, strict contract in the form of WSDL
Supports stateful calls
Response cannot be cached 
Root cause of SOAP's problems and unpopularity: XML