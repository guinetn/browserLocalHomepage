## CLS (Common Language Specification)
all objects that are created in code must rely on some commonality in the languages that are consuming them
.NET platform has specified those commonalities in something called the CLS. 
CLS is a subset of the CTS